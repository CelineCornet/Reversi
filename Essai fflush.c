

do
            {

                fflush(stdin);
            }
            while (!scanf ("%d",&lin)  && (lin<0 || lin > dimension+1) || (columns<65 || columns > dimension+65));

            do
            {

                fflush(stdin);
            }
            while (!scanf ("%c",&columns) && (columns<65 || columns > dimension+65));